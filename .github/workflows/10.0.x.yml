name: 10.0.x

on:
  schedule:
    - cron: '0 0 * * SAT'
  workflow_dispatch:
    branches:
      - master

jobs:
  retro64:
    uses: vanilla-dpup/woof-CE/.github/workflows/build.yml@vanilladpup-10.0.x
    with:
      repo: vanilla-dpup/woof-CE
      ref: vanilladpup-10.0.x
      name: "Vanilla Dpup"
      version: 10.0.${{ github.run_number }}
      arch: x86_64
      compat-distro: debian
      compat-distro-version: bookworm64
      kernel: debian-bookworm
      prefix: vanilladpup
      artifact: vanilladpup-x86_64-10.0.${{ github.run_number }}-retro
      retention: 1
      variant: retro
  xorg64:
    uses: vanilla-dpup/woof-CE/.github/workflows/build.yml@vanilladpup-10.0.x
    with:
      repo: vanilla-dpup/woof-CE
      ref: vanilladpup-10.0.x
      name: "Vanilla Dpup"
      version: 10.0.${{ github.run_number }}
      arch: x86_64
      compat-distro: debian
      compat-distro-version: bookworm64
      kernel: debian-bookworm
      prefix: vanilladpup
      artifact: vanilladpup-x86_64-10.0.${{ github.run_number }}-xorg
      retention: 1
      variant: xorg
  xfce64:
    uses: vanilla-dpup/woof-CE/.github/workflows/build.yml@vanilladpup-10.0.x
    with:
      repo: vanilla-dpup/woof-CE
      ref: vanilladpup-10.0.x
      name: "Vanilla Dpup"
      version: 10.0.${{ github.run_number }}
      arch: x86_64
      compat-distro: debian
      compat-distro-version: bookworm64
      kernel: debian-bookworm
      prefix: vanilladpup
      artifact: vanilladpup-x86_64-10.0.${{ github.run_number }}-xfce
      retention: 1
      variant: xfce
  xwayland64:
    uses: vanilla-dpup/woof-CE/.github/workflows/build.yml@vanilladpup-10.0.x
    with:
      repo: vanilla-dpup/woof-CE
      ref: vanilladpup-10.0.x
      name: "Vanilla Dpup"
      version: 10.0.${{ github.run_number }}
      arch: x86_64
      compat-distro: debian
      compat-distro-version: bookworm64
      kernel: debian-bookworm
      prefix: vanilladpup
      artifact: vanilladpup-x86_64-10.0.${{ github.run_number }}-xwayland
      retention: 1
      variant: xwayland
  dwl64:
    uses: vanilla-dpup/woof-CE/.github/workflows/build.yml@vanilladpup-10.0.x
    with:
      repo: vanilla-dpup/woof-CE
      ref: vanilladpup-10.0.x
      name: "Vanilla Dpup"
      version: 10.0.${{ github.run_number }}
      arch: x86_64
      compat-distro: debian
      compat-distro-version: bookworm64
      prefix: vanilladpup
      kernel: debian-bookworm
      artifact: vanilladpup-x86_64-10.0.${{ github.run_number }}-dwl
      retention: 1
      variant: dwl
  retro32:
    uses: vanilla-dpup/woof-CE/.github/workflows/build.yml@vanilladpup-10.0.x
    with:
      repo: vanilla-dpup/woof-CE
      ref: vanilladpup-10.0.x
      name: "Vanilla Dpup"
      version: 10.0.${{ github.run_number }}
      arch: x86
      compat-distro: debian
      compat-distro-version: bookworm
      kernel: debian-bookworm
      prefix: vanilladpup
      artifact: vanilladpup-x86-10.0.${{ github.run_number }}-retro
      retention: 1
      variant: retro
  xfce32:
    uses: vanilla-dpup/woof-CE/.github/workflows/build.yml@vanilladpup-10.0.x
    with:
      repo: vanilla-dpup/woof-CE
      ref: vanilladpup-10.0.x
      name: "Vanilla Dpup"
      version: 10.0.${{ github.run_number }}
      arch: x86
      compat-distro: debian
      compat-distro-version: bookworm
      kernel: debian-bookworm
      prefix: vanilladpup
      artifact: vanilladpup-x86-10.0.${{ github.run_number }}-xfce
      retention: 1
      variant: xfce
  upload64:
    needs: [retro64, xorg64, xfce64, xwayland64, dwl64]
    runs-on: ubuntu-20.04
    steps:
      - name: Download retro build artifact
        uses: actions/download-artifact@v3
        with:
          name: vanilladpup-x86_64-10.0.${{ github.run_number }}-retro
          path: retro
      - name: Download xorg build artifact
        uses: actions/download-artifact@v3
        with:
          name: vanilladpup-x86_64-10.0.${{ github.run_number }}-xorg
          path: xorg
      - name: Download xfce build artifact
        uses: actions/download-artifact@v3
        with:
          name: vanilladpup-x86_64-10.0.${{ github.run_number }}-xfce
          path: xfce
      - name: Download xwayland build artifact
        uses: actions/download-artifact@v3
        with:
          name: vanilladpup-x86_64-10.0.${{ github.run_number }}-xwayland
          path: xwayland
      - name: Download dwl build artifact
        uses: actions/download-artifact@v3
        with:
          name: vanilladpup-x86_64-10.0.${{ github.run_number }}-dwl
          path: dwl
      - name: Rename ISO
        run: |
          mv -f retro/*.iso vanilladpup-10.0.${{ github.run_number }}-retro.iso
          mv -f xorg/*.iso vanilladpup-10.0.${{ github.run_number }}-xorg.iso
          mv -f xfce/*.iso vanilladpup-10.0.${{ github.run_number }}-xfce.iso
          mv -f xwayland/*.iso vanilladpup-10.0.${{ github.run_number }}-xwayland.iso
          mv -f dwl/*.iso vanilladpup-10.0.${{ github.run_number }}-dwl.iso
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: vanilladpup-x86_64-10.0.${{ github.run_number }}
          tag_name: vanilladpup-x86_64-10.0.${{ github.run_number }}
          fail_on_unmatched_files: true
          files: |
            *.iso
            retro/kernel_sources-*.sfs
  upload32:
    needs: [retro32, xfce32]
    runs-on: ubuntu-20.04
    steps:
      - name: Download retro build artifact
        uses: actions/download-artifact@v3
        with:
          name: vanilladpup-x86-10.0.${{ github.run_number }}-retro
          path: retro
      - name: Download xfce build artifact
        uses: actions/download-artifact@v3
        with:
          name: vanilladpup-x86-10.0.${{ github.run_number }}-xfce
          path: xfce
      - name: Rename ISO
        run: |
          mv -f retro/*.iso vanilladpup-10.0.${{ github.run_number }}-retro.iso
          mv -f xfce/*.iso vanilladpup-10.0.${{ github.run_number }}-xfce.iso
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: vanilladpup-x86-10.0.${{ github.run_number }}
          tag_name: vanilladpup-x86-10.0.${{ github.run_number }}
          fail_on_unmatched_files: true
          files: |
            *.iso
            retro/kernel_sources-*.sfs
  cleanup:
    needs: [upload64, upload32]
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        arch: [x86_64, x86]
    steps:
      - name: Delete old ${{ matrix.arch }} releases
        uses: dev-drprasad/delete-older-releases@v0.2.0
        with:
          keep_latest: 1
          delete_tag_pattern: vanilladpup-${{ matrix.arch }}-10.0.
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
